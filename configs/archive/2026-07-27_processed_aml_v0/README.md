# Processed AML v0 configs

These configs reproduce the completed same-cell, shared-marker ablation and
literature-baseline experiments summarized in
`docs/archive/2026-07-27_processed_aml_v0/`.

They are provenance, not active landscape configs. The referenced generated
phase-0 split and row-bank artifacts were removed during the 2026-07-30
closeout and remain recoverable from checkpoint commit `8fece8b`. Rebuild or
restore those artifacts before rerunning these configs.
