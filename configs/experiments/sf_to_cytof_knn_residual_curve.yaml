extends: sf_to_cytof_paired_curve.yaml

experiment:
  name: sf_to_cytof_knn_residual_curve

training:
  residual_baseline: knn_hl

evaluation:
  alphas: [0.0, 0.025, 0.05, 0.1, 0.25, 0.5, 1.0]

output:
  save_models: false
