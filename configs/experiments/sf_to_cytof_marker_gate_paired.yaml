extends: sf_to_cytof_knn_residual_curve.yaml

experiment:
  name: sf_to_cytof_marker_gate_paired

training:
  ot:
    pairing: matched

evaluation:
  marker_gate: true
  uncertainty_diagnostics: false
  selection_pairing: matched
